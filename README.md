# PP OS — ย้ายแล้ว ⚠️

repo นี้ **ไม่ใช้แล้ว** (14 ก.ค. 2026) และ **GitHub Pages ของ repo นี้ถูกปิดแล้ว**

PP OS ถูกยุบรวมเข้าไปอยู่ใน repo ของเว็บ Moatrices เพื่อให้เป็น repo เดียว deploy เดียว:

- **โค้ดอยู่ที่:** [`beatp9696-arch.github.io`](https://github.com/beatp9696-arch/beatp9696-arch.github.io) → โฟลเดอร์ `pp-os/`
- **URL เดิมไม่เปลี่ยน:** https://beatp9696-arch.github.io/pp-os/ (ตอนนี้เสิร์ฟจาก user site แทน project page)
- เอกสารทั้งหมดย้ายไปที่ `pp-os/README.md` ใน repo นั้น

**อย่าแก้ไฟล์ใน repo นี้** — แก้แล้วจะไม่มีผลกับของจริง เก็บไว้เป็นประวัติเท่านั้น
